services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    container_name: swagger-ui
    ports:
      - "8080:8080"
    environment:
      SWAGGER_JSON: /foo/openapi.yaml
    volumes:
      - ./openapi.yaml:/foo/openapi.yaml:ro